name: TestPlugin
version: '${project.version}'
main: its.wurm.testplugin.Main
api-version: 1.17
authors: [ Wurm ]
description: learning how to do plugin stuff
commands:
  giveitem:
    description: Give an item with an optional paramiter for amount and player
    usage: /<command>
  menu:
    description: Open a custom GUI to the player
    usage: /<command>
  anvil:
    description: Open a custom GUI to the player
    usage: /<command>
  recipes:
    description: Open a custom GUI to the player
    usage: /<command>
  marker:
    description: Create a damage marker
    usage: /<command>
  summonnew:
    description: Create a Custom Mob
    usage: /<command>
  rank:
    description: set a player's rank
    usage: /<command>
  check:
    description: check player's item
    usage: /<command>
  inventory:
    description: check a player's inventory
    usage: /<command>
  editinventory:
    description: go inside a player's inventory
    usage: /<command>
  addenchant:
    description: add an enchantment to a player's inventory
    usage: /<command>
  etable:
    description: open an enchanting table gui
    usage: /<command>
  setContainer:
    description: set a persistent data container
    usage: /<command>
  sack:
    description: open a player's sacks gui
    usage: /<command>
  sacks:
    description: open a player's sacks gui
    usage: /<command>